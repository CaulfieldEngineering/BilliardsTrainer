# =========================
# CMake / build artifacts
# =========================
build/
cmake-build-*/

# CMake generated files (in-source builds)
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
compile_commands.json
CTestTestfile.cmake
Testing/
_deps/
install_manifest.txt

# =========================
# Visual Studio / MSBuild
# =========================
.vs/
.vscode/
*.suo
*.user
*.userosscache
*.sln.docstates
*.VC.db
*.VC.VC.opendb
*.vcxproj.user
*.ipch
*.tlog
*.lastbuildstate
*.recipe
*.obj
*.pdb
*.idb
*.ilk
*.log
*.pch

# Build output dirs (often produced outside `build/`)
Debug/
Release/
x64/
x86/

# =========================
# Binaries / libs
# =========================
*.exe
*.dll
*.lib
*.a
*.so
*.dylib

# =========================
# Python
# =========================
venv/
.venv/
__pycache__/
*.pyc
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
*.spec.bak

# =========================
# OS / editor noise
# =========================
Thumbs.db
Desktop.ini
.DS_Store

# Per-machine Claude Code overrides (paths/permissions differ per dev box)
.claude/settings.local.json

_local_models/

# Read-only reference clones for prior-art research (never committed)
_refs/

# Phase 0 eval: large source video + generated eval artifacts (never committed)
testVideo.*
testPhoto.*
/_eval/
docs/eval/*/

# Secrets — never commit (Roboflow key etc.)
.env
.env.local
.env.*

_ci_eval/

# external repos under evaluation (pool_coach etc.) + their isolated envs
_external/

# local debug/training artifacts (crash dumps, procdump, datasets)
_train/
*.dmp
# base YOLO weights (auto-downloaded by ultralytics during fine-tunes)
*.pt
.trainvenv/
docs/feedmeter-snaps/
_eval/
camera/
2026.08.23 - EOS DIGITAL/
